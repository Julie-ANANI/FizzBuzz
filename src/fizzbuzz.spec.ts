import { fizzBuzz } from "./fizzbuzz";

describe('FizzBuzz', () => {
    test(' should returns 1 when 1 in as input', () => {
        // Arrange
        // les starts data

        // Act
        // execute an mutation

        // Assert
        //test result

    });
});
